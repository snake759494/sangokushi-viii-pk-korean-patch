# 실행 확인 기록

PCSX2 에서 한글 BIN 을 실행해 찍은 스크린샷(2026-09-21, 144장)을 빌드마다 살펴보고 빠진 곳을 고쳤습니다.
스크린샷은 게임 화면이라 저장소에 넣지 않았습니다. 확인은 스크린샷을 찍은 **그 시점의 빌드** 기준입니다.

## 빌드별로 찾아 고친 것

| 스크린샷 (빌드) | 화면에서 본 것 | 고친 내용 |
| --- | --- | --- |
| 13:47~14:05 (1차) | 한글이 위로 치우쳐 글자 아래에 다음 글자 윗부분이 점처럼 보임 | 게임이 글리프를 2행부터 찍는 것을 확인하고 기준선을 19 → 21 로 내림 |
| 〃 | 무장 이름·도시 이름이 깨진 한자(公孫度 등) | `D_SCE.S8` 무장 658명·이름 칸 번역, 가나 읽기 비움 |
| 14:49~15:01 (2차) | 「198품 10룽」(年·月 미번역), `%d` 가 그대로 찍힘 | 실행파일의 한 글자·반각 가나·서식 문자열 816개를 더 찾아 번역, 대사 속 `%` 는 전각 `％` |
| 〃 | 시나리오 선택·개요 글이 상자 밖으로 넘침 | 단위별 줄 폭 규칙(원문 그 단위의 가장 긴 줄 기준)으로 2,498단위를 다시 나눔 |
| 〃 | 「△はい」 | 예/아니요 단추를 아이콘 포함 `△예`/`×아니요` 로 |
| 〃 | 情報·進行·地図 안내판, 君主 배지, 行·戦 표식이 일본어 그림 | `G_MAIN.S8` 압축 그림을 풀어 다시 그림 |
| 17:22~17:36 (3차) | 濮陽·洛陽 등 도시 이름 일부가 한자 | `D_SCE.S8` 도시 표 50곳을 통째로 씀 |
| 〃 | 책략 수량 입력 「数を入力してください」 | 실행파일 문자열 추가 |
| 〃 | 전투 정보창 兵士·部隊·戦術·軍議P·攻·守·総·参, 일기토 武力·気力·体力·残りターン | `G_GUNGI`·`G_WARGRP`·`G_IKBG` 다시 그림 |
| 〃 | 적벽 지도 이벤트가 「E 없다」, 「다 / 잘 상황을…」 처럼 앞이 잘림 | 위치 인자(아래) 보정 |
| 20:33~20:51 (4차) | 오프닝 문장 앞 「ト」(ㅏ 처럼 보임) | 위치 인자 보정 |
| 〃 | 전투 부대 위 委中, 「전술 캠페인 이력을」 뒤가 없는 확인창 | 부대 상태 표시 다시 그림, 첫 줄만 남은 여러 줄 문자열 79개 다시 번역 |

위치 인자: M_MSG 의 일부 제어 코드는 인자로 **그룹 안 바이트 위치**를 가집니다. 번역으로 길이가 바뀐 뒤에도
옛 위치를 가리켜 글자 중간부터 찍히던 것이 「E 없다」(「이제」의 둘째 바이트부터), 「R모두의」(`@R` 의 R 부터)의
원인이었습니다. 1,050개를 새 위치로 옮기고 1,136개 모두 원문과 같은 명령을 가리키는지 검사했습니다.

## 화면으로 확인한 것 (각 시점 빌드)

타이틀 메뉴·HELP·튜토리얼, 시대·연대 선택(제목·연도·사건), 무장·세력·도시 선택 목록과 개요, 평정 대사와
설명 확인창, 방침·군단 방침·등용·출진 편성·업무 결정 창, 세이브·로드·환경 설정, 무장 편집(수치·특기·전법·성장 경향),
도시 일람·도시 편집·도시 정보, 연표, 전투(군의 화면 전황, 전투 맵 명령·책략 선택), 일기토, 시나리오 오프닝 문장.

## v1.0.1 제보 반영 (2026-09-23)

| 제보 (팬 카페 스크린샷) | 확인·조치 |
| --- | --- |
| 지도 범례 「도로 종류·보통·하천·산악」 한글이 깨짐 | v1.0 이 팔레트를 몰라 색 번호를 추정해 그린 탓. 게임 팔레트(G_SYSTEM 0x8400)를 찾아, 그 팔레트로 원본 범례가 정확히 재현되는 것(흰 글자, 군단 색 견본, 회색·청록·주황 선)을 확인한 뒤 원본 글자와 같은 회색 단계 색으로 다시 그림 |
| 지도의 세력 색이 모두 같아 보임 (도시 칸이 모두 노란색) | 그림을 고치기 전 빌드의 스크린샷에서도 도시 선택 화면은 노란색(고를 수 있는 도시)·회색(고를 수 없는 도시)이었고, 같은 빌드의 주 선택 지도는 세력 색으로 나옴 → 원래 동작으로 판단. 실행파일에서 번역한 문자열 중 색 데이터(RGBA)로 보이는 것이 없음도 확인 |

같은 방식으로 다른 그림 글자의 팔레트도 다시 확인했습니다. 전략 지도 주 이름(G_EMTMP)은 실제 팔레트(블록 14 의 0번)로 그려도 원본과 같은 크림색 글씨·어두운 그림자로 나옵니다.

## v1.0 에서 화면으로 다시 확인하지 못한 것

마지막 빌드에서 고친 다음 항목은 빌드 검사와 그림 미리보기로만 확인했습니다.

- 위치 인자 보정 (적벽 이벤트·오프닝·지도 이벤트 대사 넘김)
- 여러 줄 확인창 79개 (세이브·로드·무장 편집·설치·전술 캠페인 이력 등)
- 전투 부대 상태 표시 도발·위임·지시, 총대장·참모 총·참, 부대 종류 정·증·응·방
- 업무 명령·결과 판, 전략 지도의 주 이름, 도시 편집의 주 읽기 비움

전 시나리오 완주·엔딩, 실기 PS2 는 확인하지 않았습니다.
